imports:
    - { resource: config.yml }
    - { resource: parameters_prod.yml }

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: nested
        nested:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: debug
        console:
            type: console
            process_psr_3_messages: false

sentry:
    dsn: "http://cebfb0dd9ec5499aaf41b811b2dac8aa@sentry.presta-studio.ru/2"
